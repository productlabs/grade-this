Run:
yarn install

To Preview:
gatsby develop

To Build 
gatsby build
gatsby serve


To pull latest changes: (always good to do first)
git pull

To save changes:
git add -A
git commit -m "what you did"
git push
